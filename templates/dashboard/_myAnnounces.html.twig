<div class="container">
    <div class="section d-flex align-items-center flex-column">
        <h1>Mes annonces en cours</h1>
            <a class="btn btn-primary" href="{{ path('annonce_new') }}">
                Ajouter une annonce [+]
            </a>
        </div>
    <div class="section">
        <div class="grid">
            {% for annonce in annonces %}
                {% include '/annonce/_annonceMin.html.twig' %}
            {% else %}
                No records found
            {% endfor %}
        </div>
    </div>
</div>
