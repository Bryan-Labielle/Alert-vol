<div class="col-md-4 col-sm-6 mb-4" >
        <div class="card card-align p-0 z-index-1">
            <div class="d-block">
                <img class="card-img-top" src="{{ apiImages.hits[loop.index].previewURL }}" alt='title picture'>
                <div class="d-flex align-items-start justify-content-end ">
                    <i class="fas fa-bookmark bookmark-position">
                    </i>
                </div>
            </div>
                <div class="card-header custom-header  p-0 d-flex justify-content-center">
                    <a href="{{ path('annonce_show', {'slug':annonce.slug}) }}" class="text-start fs-4 text-decoration-none stretched-link" style="color: #212526">{{ annonce.title|title }}</a>
                </div>
            <div class="ligne-title m-auto"></div>
                <div class=" reference fs-5 text-center"> {{ annonce.reference | upper }}</div>
                <a href="{{ path('annonce_show', {'slug':annonce.slug}) }}" class="details-card align-self-end">Voir plus</a>

            <div class="div card-bottom-blue">
                <div class="container-info d-flex justify-content-between ">
                    <div class="details-annonce col-5"><i class="far fa-calendar-alt mx-2"></i>Perdu le : {{ annonce.stolenAt|date("m/d/Y") }}</div>
                    <div class="details-white details-annonce col-3"> Ville, {{ annonce.location }}</div>
                </div>
                <div class="text-center m-auto">
                    <a class="btn btn-circle" style="z-index: 3; font-weight: 700;" href="{{ path('annonce_signalement', {slug: annonce.slug}) }}">
                        J'ai vu ce véhicule <i class="fas fa-bullhorn" style="color: #f94a4a"></i></a>
                </div>
            </div>
        </div>
</div>
