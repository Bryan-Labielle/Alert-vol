{% extends 'base.html.twig' %}

{% block body %}
    <main>
{#{{ include('annonce/_annonceSignalement.html.twig') }}#}
        <div class="container mt-5">
            <div class="row mb-5">
                <div class="col-md-6">
                    <div class="card-header custom-header fs-4  p-0 d-flex justify-content-center">
                        {{ annonce.title|title }}
                    </div>
                    <div class=" reference fs-5 text-center"> {{ annonce.reference | upper }}</div>
                    <div class="container-info-signalement d-flex justify-content-between ">
                        <div class="details-annonce col-5"><i class="far fa-calendar-alt mx-2"></i>Perdu le : {{ annonce.stolenAt|date("m/d/Y") }}</div>
                        <div class="details-white details-annonce col-3"> Ville, {{ annonce.location }}</div>
                    </div>
                </div>
                <div class="col-md-6">

                    map
                </div>
            </div>
            <hr>
            {% include 'signalement/_form-signalement.html.twig' with {edit: false} %}
{#            <img class="card-img-top" src="{{ apiImages.hits.1.previewURL }}" alt='title picture'>#}

        </div>
{#        <div class="col-md-6 m-auto">#}

{#            #}
{#            <div role="list" class="img-show-annonce col-md-6">#}
{#                <div class="row">#}
{#                    {% for signalementImage in signalement.signalementImages %}#}
{#                        <figure class="img-galery col-sm">#}
{#                            <div class="button-delete-img">#}
{#                                <form method="post" action="{{ path('annonce_deleteImage', {'id': signalementImage.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">#}
{#                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalementImage.id) }}">#}
{#                                    <button class="btn btn-danger">Delete</button>#}
{#                                </form>#}
{#                            </div>#}
{#                            <img class="img-thumbnail rounded galery-img" src="{{ vich_uploader_asset(signalementImage) }}" alt="{{ signalementImage.name }}">#}
{#                            <figcaption class="font-italic">Dernière mise à jour : {{ signalementImage.updatedAt|date('Y-m-d') }}#}
{#                            </figcaption>#}
{#                        </figure>#}
{#                    {% endfor %}#}
{#                </div>#}
{#            </div>#}
{#        </div>#}
        <script>
            
        </script>

    </main>
{% endblock %}
