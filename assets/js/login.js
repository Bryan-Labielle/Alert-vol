/*
const formLoginLabel = document.querySelectorAll('.login-label');
const formInputEmail = document.getElementById('inputEmail');
const formInputPassword = document.getElementById('inputPassword');

formInputEmail.addEventListener('focus', () => {
    formLoginLabel[0].style.fontSize = '0.6rem';
    formLoginLabel[0].style.transform = 'translate(20px,15px)';
    formInputEmail.style.backgroundColor = '#E1E1E1';
});

formInputEmail.addEventListener('focusout', () => {
    formLoginLabel[0].style.fontSize = '1rem';
    formLoginLabel[0].style.transform = 'translate(20px,30px)';
    formInputEmail.style.backgroundColor = '#F1F1F1';
});

formInputPassword.addEventListener('focus', () => {
    formLoginLabel[1].style.fontSize = '0.6rem';
    formLoginLabel[1].style.transform = 'translate(20px,15px)';
    formInputPassword.style.backgroundColor = '#E1E1E1';
});

formInputPassword.addEventListener('focusout', () => {
    formLoginLabel[1].style.fontSize = '1rem';
    formLoginLabel[1].style.transform = 'translate(20px,30px)';
    formInputPassword.style.backgroundColor = '#F1F1F1';
});
*/
